<a class="link" href="/post/{{id}}"></class>
  <div class="card">

    <div class="card-header">
      <h4>{{title}}</h4>
      <p class="meta">Blog by {{user.username}} on {{created_at}}</p>
    </div>
    <p class="card-body">{{content}}</p>

  </div>
</a>
